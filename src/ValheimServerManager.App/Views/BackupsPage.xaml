<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ValheimServerManager.App.Views.BackupsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="using:ValheimServerManager.App.Helpers"
    xmlns:vm="using:ValheimServerManager.App.ViewModels">

    <Grid MaxWidth="{StaticResource PageMaxWidth}" Padding="{StaticResource PagePadding}" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Style="{StaticResource PageTitleStyle}" Text="Backups" />
            <TextBlock Margin="0" Style="{StaticResource PageSubtitleStyle}" Text="{x:Bind ViewModel.Summary, Mode=OneWay}" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button AutomationProperties.Name="Fazer backup agora" Command="{x:Bind ViewModel.BackupNowCommand}" Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE8F1;" />
                    <TextBlock Text="Fazer backup agora" />
                </StackPanel>
            </Button>
            <Button Grid.Column="1" AutomationProperties.Name="Abrir pasta de backups" Command="{x:Bind ViewModel.OpenRootCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE838;" />
                    <TextBlock Text="Abrir pasta" />
                </StackPanel>
            </Button>
            <Button Grid.Column="2" AutomationProperties.Name="Atualizar" Command="{x:Bind ViewModel.RefreshCommand}" ToolTipService.ToolTip="Atualizar">
                <FontIcon FontSize="16" Glyph="&#xE72C;" />
            </Button>
            <ToggleSwitch Grid.Column="4" IsOn="{x:Bind ViewModel.ShowAllWorlds, Mode=TwoWay}" OffContent="Só este mundo" OnContent="Todos os mundos" />
        </Grid>

        <StackPanel Grid.Row="2" Spacing="8">
            <StackPanel Spacing="4" Visibility="{x:Bind helpers:Ui.Visible(ViewModel.IsBusy), Mode=OneWay}">
                <ProgressBar IsIndeterminate="True" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind ViewModel.BusyText, Mode=OneWay}" />
            </StackPanel>
            <InfoBar
                IsClosable="True"
                IsOpen="True"
                Message="Cada backup guarda um save completo e é conferido byte a byte (SHA-256). Restaurar nunca mistura arquivos de saves diferentes e nunca apaga o mundo atual: ele é copiado antes e movido para _substituidos."
                Severity="Informational" />
        </StackPanel>

        <Grid Grid.Row="3">
            <TextBlock
                Margin="4,24"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Nenhum backup para mostrar."
                Visibility="{x:Bind helpers:Ui.VisibleIfNone(ViewModel.Items.Count), Mode=OneWay}" />
            <ListView ItemsSource="{x:Bind ViewModel.Items}" SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,6" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:BackupItemViewModel">
                        <Border Padding="16,12" Style="{StaticResource CardBorderStyle}">
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <FontIcon VerticalAlignment="Center" FontSize="22" Glyph="{x:Bind Glyph}" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind Title}" />
                                        <TextBlock VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind When}" />
                                    </StackPanel>
                                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind Details}" />
                                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind Note}" TextWrapping="Wrap" Visibility="{x:Bind helpers:Ui.VisibleIfText(Note)}" />
                                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind VerifyText, Mode=OneWay}" TextWrapping="Wrap" Visibility="{x:Bind helpers:Ui.VisibleIfText(VerifyText), Mode=OneWay}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" Orientation="Horizontal" Spacing="6">
                                    <Button Click="OnRestoreClick" Content="Restaurar" Tag="{x:Bind}" Visibility="{x:Bind helpers:Ui.Visible(CanRestore)}" />
                                    <Button Click="OnVerifyClick" Content="Verificar" Tag="{x:Bind}" />
                                    <Button AutomationProperties.Name="Mais opções" Tag="{x:Bind}">
                                        <FontIcon FontSize="14" Glyph="&#xE712;" />
                                        <Button.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem Click="OnOpenClick" Tag="{x:Bind}" Text="Abrir pasta">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE838;" />
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem Click="OnDeleteClick" IsEnabled="{x:Bind CanDelete}" Tag="{x:Bind}" Text="Excluir">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE74D;" />
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </Button.Flyout>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
